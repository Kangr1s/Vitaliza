---
// ...existing code...
interface Persona {
  img: string;
  nombre: string;
  profesion: string;
  facebook?: string;
  twitter?: string;
  instagram?: string;
  linkedin?: string;
}

// Annotate equipo with the correct type
const { equipo = [] } = Astro.props as { equipo?: Persona[] };
// ...existing code...
---

<div class="container-fluid team py-5">
  <div class="container py-5">
    <div class="section-title mb-5 wow fadeInUp" data-wow-delay="0.1s">
      <div class="sub-style">
        <h4 class="sub-title px-3 mb-0">Conoce a nuestro equipo</h4>
      </div>
      <h1 class="display-3 mb-4">
        Servicios de fisioterapia de terapeutas profesionales
      </h1>
      <p class="mb-0">
        Contamos con un equipo altamente capacitado en diferentes áreas de la fisioterapia. 
        Nuestro compromiso es brindarte un acompañamiento profesional y humano para que 
        recuperes tu movilidad, alivies el dolor y mejores tu calidad de vida. 
        Cada tratamiento está diseñado de manera personalizada para adaptarse a tus necesidades.
      </p>
    </div>
    <div class="row g-4 justify-content-center">
        {
        equipo.map((persona) => (
            <div
            class="col-md-6 col-lg-6 col-xl-3 wow fadeInUp"
            data-wow-delay="0.1s"
            >
            <div class="team-item rounded">
                <div class="team-img rounded-top h-100">
                <img
                    src={persona.img}
                    class="img-fluid rounded-top w-100"
                    alt={`Foto de ${persona.nombre}`}
                />
                <div class="team-icon d-flex justify-content-center">
                    <a
                    class="btn btn-square btn-primary text-white rounded-circle mx-1"
                    href={persona.facebook}
                    title="Facebook"
                    >
                    <i class="fab fa-facebook-f" />
                    </a>
                    <a
                    class="btn btn-square btn-primary text-white rounded-circle mx-1"
                    href={persona.twitter}
                    title="Twitter"
                    >
                    <i class="fab fa-twitter" />
                    </a>
                    <a
                    class="btn btn-square btn-primary text-white rounded-circle mx-1"
                    href={persona.instagram}
                    title="Instagram"
                    >
                    <i class="fab fa-instagram" />
                    </a>
                    <a
                    class="btn btn-square btn-primary text-white rounded-circle mx-1"
                    href={persona.linkedin}
                    title="LinkedIn"
                    >
                    <i class="fab fa-linkedin-in" />
                    </a>
                </div>
                </div>
                <div class="team-content text-center border border-primary border-top-0 rounded-bottom p-4">
                <h5>{persona.nombre}</h5>
                <p class="mb-0">{persona.profesion}</p>
                </div>
            </div>
            </div>
        ))
        }
    </div>
    </div>
</div>
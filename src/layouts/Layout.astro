---
import Header from "../components/Header.astro";
import Footer from "../components/Footer.astro";
import Breadcrumb from "../components/BreadCrumb.astro";
import { routes } from "../data/breadcrumbs";
import { blogPosts } from "../data/blogPosts";

interface Props {
  title?: string;
  description?: string;
}

const { title, description } = Astro.props;
const path = Astro.url.pathname;
let current = routes[path];

// Si no encuentra coincidencia exacta, verificamos si es un post del blog
if (!current && path.startsWith("/blog/")) {
  const slug = path.split("/").pop();
  const post = blogPosts.find((p) => p.slug === slug);

  if (post) {
    current = {
      title: post.title,
      items: [
        { name: "Inicio", url: "/" },
        { name: "Blog", url: "/blog" },
        { name: post.title },
      ],
    };
  }
}
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <script
      src="https://kit.fontawesome.com/58deb355db.js"
      crossorigin="anonymous"></script>
    <link rel="icon" type="image/png" href="img/v.png" />
    <!-- Google Web Fonts -->
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Open+Sans:wght@400;500;600&family=Playfair+Display:wght@400;500;600&display=swap"
      rel="stylesheet"
    />

    <!-- Icon Font Stylesheet -->
    <link
      rel="stylesheet"
      href="https://use.fontawesome.com/releases/v5.15.4/css/all.css"
    />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.4.1/font/bootstrap-icons.css"
      rel="stylesheet"
    />

    <!-- Libraries Stylesheet -->
    <link href="/lib/animate/animate.min.css" rel="stylesheet" />
    <link
      href="/lib/owlcarousel/assets/owl.carousel.min.css"
      rel="stylesheet"
    />

    <!-- Customized Bootstrap Stylesheet -->
    <link href="/css/bootstrap.min.css" rel="stylesheet" />

    <!-- Template Stylesheet -->
    <link href="/css/style.css" rel="stylesheet" />
    <meta name="generator" content={Astro.generator} />
    <title>{title}</title>
  </head>
  <body>
    <Header />
    {path !== "/" && current && <Breadcrumb route={current} />}
    <slot />
    <Footer />
    <!-- Librerías JS (en orden, jQuery primero) -->
    <script src="/js/jquery-3.7.0.min.js" is:inline></script>
    <script src="/js/bootstrap.min.js" is:inline></script>
    <script src="/lib/wow/wow.min.js" is:inline></script>
    <script src="/lib/easing/easing.min.js" is:inline></script>
    <script src="/lib/waypoints/waypoints.min.js" is:inline></script>
    <script src="/lib/owlcarousel/owl.carousel.min.js" is:inline></script>
    <script src="/js/main.js" is:inline></script>
  </body>
</html>
